# define optional defaults

#config rate
#	option name 'mydefaults'	# identifier for these defaults, used for "defaults" ubus parameter
#	option egress '10000000'	# use a value compatible with tc htb rate (numerical or suffixed)
#	option ingress '20mbit'		# use a value compatible with tc htb rate (numerical or suffixed)
